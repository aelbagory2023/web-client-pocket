FROM node:18

WORKDIR /usr/src/app

ARG RELEASE_VERSION

COPY . .

ENV NEXT_TELEMETRY_DISABLED 1
ENV NODE_ENV=production
ENV PORT 80
ENV RELEASE_VERSION=${RELEASE_VERSION}

EXPOSE ${PORT}

CMD ["npm", "start"]
