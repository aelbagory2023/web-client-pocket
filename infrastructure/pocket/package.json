{
  "name": "pocket-client-cdk",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "types": "src/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "setup": "brew install tfenv && tfenv install 1.8.3 && tfenv use 1.8.3",
    "build:dev": "npm run build && NODE_ENV=development npm run synth",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w"
  },
  "engines": {
    "node": "=20.10.0"
  },
  "dependencies": {
    "@cdktf/provider-archive": "10.0.1",
    "@cdktf/provider-aws": "19.21.0",
    "@cdktf/provider-local": "10.1.0",
    "@cdktf/provider-null": "10.0.0",
    "@cdktf/provider-pagerduty": "13.8.2",
    "@pocket-tools/terraform-modules": "5.12.5",
    "cdktf": "0.20.7",
    "cdktf-cli": "0.20.7",
    "constructs": "10.3.0",
    "tslib": "2.6.3"
  },
  "devDependencies": {
    "@types/node": "20.14.8"
  }
}