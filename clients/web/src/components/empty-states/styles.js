import { css } from '@emotion/css'

export const emptyStyles = css`
  text-align: center;
  img {
    margin: 52px 0;

    &.library {
      max-width: 400px;
    }
  }

  h4 {
    font-weight: 600;
    margin-bottom: 16px;
  }

  button {
    color: var(--color-textPrimary);
  }
`
